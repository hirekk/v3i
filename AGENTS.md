# AGENTS.md

Agent instructions for the **v3i** repo (hypercomplex peel-and-solve neural
networks). See `README.md` for the project itself.

## Agent skills

### Issue tracker

Issues, PRDs, and wayfinder maps live in **GitHub Issues** (`hirekk/v3i`), via the
`gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage labels, used as-is (`needs-triage`, `needs-info`,
`ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See
`docs/agents/domain.md`.
